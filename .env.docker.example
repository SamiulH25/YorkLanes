# Optional overrides for docker compose (copy to .env in repo root).
# apps/api/.env is still required for database and OAuth secrets.

WEB_ORIGIN=https://yorklanes.SamiulH25.com
PUBLIC_API_URL=https://yorklanes.SamiulH25.com
GOOGLE_CALLBACK_URL=https://yorklanes.SamiulH25.com/api/auth/google/callback

# If ports 80/443 are already in use on this machine, set alternates:
# HTTP_PORT=8080
# HTTPS_PORT=8443
