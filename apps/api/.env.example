# Copy to apps/api/.env — get real values from the database maintainer.

# Shared team Postgres (required)
SUPABASE_DB_URL=

API_PORT=3001
API_BASE_URL=http://localhost:3001
WEB_ORIGIN=http://localhost:4321

# Optional: path to venv python if not on PATH (degree plan checklist import)
# PYTHON_PATH=services/checklist-parser/.venv/Scripts/python.exe

# Maintainer / production only (not needed for feature dev)
# SESSION_SECRET=
# SUPABASE_SERVICE_ROLE_KEY=
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
