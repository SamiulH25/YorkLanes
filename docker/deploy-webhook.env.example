# Copy to docker/deploy-webhook.env and set a long random secret.
# Use the same value in GitHub → Repo → Settings → Webhooks → Secret.

GITHUB_WEBHOOK_SECRET=change-me-to-a-long-random-string
WEBHOOK_PORT=9876
WEBHOOK_BIND=127.0.0.1
